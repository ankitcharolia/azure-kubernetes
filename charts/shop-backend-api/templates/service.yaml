apiVersion: v1
kind: Service
metadata:
  name: {{ include "shop-backend-api.fullname" . }}
  labels:
    {{- include "shop-backend-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shop-backend-api.selectorLabels" . | nindent 4 }}
